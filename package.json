{
  "name": "champions-ledger",
  "private": true,
  "description": "Champions Ledger: a box, build and team manager for Pokemon Champions. This manifest exists so the browser tests can run in CI - they load tracker/dist/index.html under jsdom, and without it the daily job had no way to install jsdom, which is why nothing gated on them.",
  "scripts": {
    "test": "node tests/pagetest.js"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
