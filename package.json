{
  "name": "pokemon-champions-ledger",
  "private": true,
  "description": "Tooling for the Champions ledger. Exists so the browser tests can run in CI: they load tracker/dist/index.html under jsdom, and without a manifest the daily job had no way to install it - which is why nothing gated on them.",
  "scripts": {
    "test": "node tests/pagetest.js"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
